{{#if shouldDisplay}}
  {{d-button label=(theme-prefix (if codeReviewAssistant.active "stop" "open")) action=(action "toggleActive") class=(if codeReviewAssistant.active "btn-primary") icon="fab-github"}}
{{/if}}